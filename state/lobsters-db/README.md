# Lobsters With Postgres Container Image

To build, drop these files with [Lobsters](https://github.com/jcs/lobsters).

Default login is test/test

Expects postgres host/pass as env vars `DB_PASSWORD` and `DB_HOST`.
